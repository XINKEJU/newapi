# 2026-06-25 — 俄罗斯本土化 P0-P2 修复

## P0-1: YandexGPT & GigaChat 适配器
- `common/api_type.go`: 显式注册 YandexGPT(58) 和 GigaChat(59) 映射到 APITypeOpenAI
- `relay/channel/openai/adaptor.go`: 添加 YandexGPT 专用认证头 `Authorization: Api-Key <key>`（YandexGPT 使用 Api-Key 而非 Bearer）
- `relay/common/relay_info.go`: 将 YandexGPT 和 GigaChat 加入 `streamSupportedChannels`

## P1-3: 前端时区支持
- `web/default/src/lib/dayjs.ts`: 安装 `dayjs/plugin/utc` 和 `dayjs/plugin/timezone` 插件，默认时区设为 `Europe/Moscow`

## P1-6: 前端硬编码中文修复
- `common/gin.go`: `ApiErrorI18n()` 响应新增 `"key"` 字段（向后兼容）
- `web/default/src/routes/oauth/$provider.tsx`: 用 `errorKey === 'oauth.already_bound'` 替代硬编码 `'该 GitHub 账户已被绑定'`

## P2-7: 俄语 README
- 新建 `README.ru.md`：完整俄语 README，含俄罗斯特有功能（YooMoney, SberPay, Yandex/VK OAuth, YandexGPT, GigaChat）
- `README.en.md`: 添加俄语链接，多语言描述加入 Russian

## P2-10: 数字格式化显式 ru-RU locale
- `web/default/src/lib/currency.ts`: `Intl.NumberFormat(undefined, ...)` → `Intl.NumberFormat('ru-RU', ...)` 两处

## 第二批修复 (17:13 - )

### P1-5: 经典主题 RU 补全 + fallbackLng
- `web/classic/src/i18n/i18n.js`: `fallbackLng: 'zh-CN'` → `'en'`（俄语用户看英文比中文更好）
- `web/classic/src/i18n/locales/ru.json`: 补充 49 个核心 UI 翻译（按钮/标签/表单字段），从 3702 → 3751 keys
- 剩余 118 个缺失 key（多为阶梯计费等复杂功能）回退到英文显示

### P2-11: YooMoney API Key 注释修正
- `setting/payment_yoomoney.go`: 更新 YoomoneyApiKey 注释，明确其当前未用于 webhook 签名验证（实际签名验证使用 YoomoneyNotifySecret 的 SHA-1），标注为预留字段

### P2-8 / P2-9: 推迟
- **Kandinsky**: API 完全不对齐 OpenAI（队列提交+轮询模式），需要完整的自定义适配器，类似 Midjourney。推迟。
- **Mail.ru OAuth**: Mail.ru 已于 2022 年并入 VK ID 生态，独立的 Mail.ru OAuth 已弃用。VK OAuth 已覆盖该用户群。跳过。

## 第三批修复 (17:20 - )

### P1-4: 邮件模板系统
- `i18n/email_template.go`: 模板引擎 + go:embed 嵌入 `i18n/email_templates/`，`RenderEmailTemplate()` 和 `SendTemplateEmail()`
- `i18n/email_templates/{ru,en,zh}/email_verification.html` — 邮箱验证模板（3 语言）
- `i18n/email_templates/{ru,en,zh}/password_reset.html` — 密码重置模板（3 语言）
- `i18n/email_templates/{ru,en,zh}/notification.html` — 通用通知模板（3 语言）
- `i18n/keys.go` + 4 个 locale yaml 文件：新增 `email.verification_subject`、`email.password_reset_subject`、`email.test_subject` 三条目
- `controller/misc.go`: SendEmailVerification 和 SendPasswordResetEmail 改用模板引擎，硬编码中文主题替换为 i18n
- 注意：`service/user_notify.go` 的 sendEmailNotify 保持原样（系统通知无 gin context）

### P0-2: SMS 验证系统 (SMS.ru)
- `setting/sms.go`: SMS 配置（SMS_ENABLED, SMS_PROVIDER, SMS_API_ID, SMS_SENDER）
- `common/sms.go`: SMS.ru HTTP API 封装，支持国际电话格式
- `model/sms_verification.go`: `sms_verifications` 表（phone, code, purpose, expires_at, used）+ Record/Verify/Cleanup 方法
- `controller/sms.go`: `POST /api/sms/send` + `POST /api/sms/verify` 端点，含限速+Turnstile 中间件
- `router/api-router.go`: 注册 SMS 路由
- `model/main.go`: AutoMigrate 加入 SMSVerification 表

## YooMoney 功能网关测试 (17:35 - )

### 测试结果
- `GET /api/status` ✅ 正常
- `POST /api/yoomoney/topup` ✅ 路由正常（需要登录态，返回 "id 为空" 符合预期）
- `POST /api/yoomoney/notify` ✅ 无参数返回 "fail"，GET 也正常
- `GET /api/yoomoney/return` ✅ 302 重定向到 `/console/wallet?pay=fail`
- `GET /api/yoomoney/subscription/return` ✅ 复用 YooMoneyReturn

### 修复
- `service/yoomoney.go`: 移除无用的 init() 函数和 time 导入
- `controller/yoomoney_webhook.go`: 新增 `unaccepted=true` 处理（YooMoney 支付未完成状态），避免重复处理

## VK/Yandex OAuth 测试 (17:41 - )
- 所有端点可达，错误消息为俄语
- CSRF state 保护正确，无效 state/无 code 均正确拒绝
- `oauth/yandex.go`: 修复缺失的 redirect_uri 参数（token 交换时）

## 首页默认俄语 (17:44 - )
- `web/default/index.html`: `<html lang="en">` → `<html lang="ru">`
- `web/classic/index.html`: `<html lang="zh">` → `<html lang="ru">`
- `web/default/src/i18n/config.ts`: 首次访问（无 localStorage）强制默认俄语
